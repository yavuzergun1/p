import { AbstractUserInterface, UserInterface, LoginContext, Cancelable, UserInterfaceLoginResponse, PromptArgs, PromptResponse, UserInterfaceTranslateOptions } from '@wharfkit/session';

interface WebUIRendererOptions {
    id?: string;
    translations?: Record<string, Record<string, string>>;
}
declare const defaultWebUIRendererOptions: {
    id: string;
};
declare class WebUIRenderer extends AbstractUserInterface implements UserInterface {
    static version: string;
    elementId: string;
    element: Element;
    shadow: ShadowRoot;
    i18n: any;
    constructor(options?: WebUIRendererOptions);
    appendDialogElement(): void;
    addCancelablePromise: (promise: any) => void;
    log(...args: any[]): void;
    login(context: LoginContext): Cancelable<UserInterfaceLoginResponse>;
    onError(error: Error): Promise<void>;
    onLogin(): Promise<void>;
    onLoginComplete(): Promise<void>;
    onTransact(): Promise<void>;
    onTransactComplete(): Promise<void>;
    onSign(): Promise<void>;
    onSignComplete(): Promise<void>;
    onBroadcast(): Promise<void>;
    onBroadcastComplete(): Promise<void>;
    prompt(args: PromptArgs): Cancelable<PromptResponse>;
    status(message: string): void;
    translate(key: string, options?: UserInterfaceTranslateOptions, namespace?: string): any;
    addTranslations(translations: any): void;
}

export { WebUIRenderer, WebUIRendererOptions, WebUIRenderer as default, defaultWebUIRendererOptions };
